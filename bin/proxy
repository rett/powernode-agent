#!/usr/bin/env ruby
require 'daemons'
basedir = File.join(File.dirname(__FILE__), '..')

daemon_options = {
  :app_name   => "proxy",
  :dir        => "#{basedir}/../tmp",
  :log_dir    => "#{basedir}/../log",
  :monitor    => true
}

Daemons.run("#{basedir}/lib/proxy.rb", daemon_options)
